# 內核 v2 · 骨架設計

> 這份文件描述 `kernel.js`（純邏輯內核）、`kernel-sim.js`（平衡模擬器）
> 與 `play.html`（可玩的試玩頁）。**舊的 `index.html` 一個字沒動。**
>
> ```bash
> node kernel-sim.js 1000    # 平衡模擬
> node play-smoke.js         # 載入期冒煙測試
> python3 -m http.server 8000 && open http://localhost:8000/play.html
> ```
>
> `play.html` 用 `<script src="kernel.js">` 載入內核——邏輯只有一份，
> 模擬器測的就是頁面跑的那份程式。代價是 CSP 的 `script-src 'self'`
> 在 `file://` 底下比對不到來源，直接雙擊會載不到內核（頁面會告訴你怎麼跑）。
> 部署到靜態主機不受影響。

---

## 為什麼要換骨架

現在的年循環是 `phaseIncome → phaseDice → phaseEvent → phasePosition → settle → postSettle`。
讀完之後有四個結構性問題，其中三個是機制層面的，一個是工程層面的。

**一、`settle()` 是擴展性的天花板**（`index.html:2177`）

約 120 行，把 15 種特例硬折疊在一連串 `if` 裡：alpha 基底、紀律懲罰、心態懲罰、
4 個特質分支、5 個流派分支、換手稅費查表、3 個習慣分支、崩盤三態、斷頭判定。
加任何內容都得動這一個函式，而且全部是寫死的魔術數字。

**二、部位每年歸零重選**（`POSN`，`index.html:645`）

沒有成本價、沒有持股延續。所以股票遊戲最核心的那些情緒——套牢、停利、賣飛、攤平、
「去年的航運我還抱著」——在機制上完全不存在。

而且 `mult: 0 / 0.72 / 1.0 / 1.25 / 1.75 / 2.55` 本質上就是一條風險滑桿，
正是作者自己在事件卡註解裡批評過的「難度旋鈕」（`index.html:723`）：
把同一件事縮放六次，跟標的講什麼沒有關係。

**三、隨機性只有結果隨機，沒有情境隨機**

骰子點數 → +5 熟練度、事件 `p%` 成敗、`m.ret`。玩家只能承受，不能反應。
唯一的資訊管道 `hintAcc()` 封頂 82%，輸出一句「偏多／偏空」——
資訊無法累積、無法解讀、也無法跨局學習。

**四、名詞太多**

習慣 10 + 特質 10 + 流派 5 + 部位 6 + 階級 4 + 受託 5 + 心態／生活／信任／名聲 + 職業 5。
每年跨四個異質系統各做一次決定，但每個系統只有一層深。

---

## 新骨架：四個名詞

| 名詞 | 是什麼 |
|---|---|
| **帳本 `book`** | 持股，跨年延續，有成本價。改變它要花行動、繳稅、把未實現變成已實現 |
| **行動點 `ap`** | 每年 3～6 點。骰子買的是「決定」，不是「數字」 |
| **牌 `move`** | 買／賣／融資／養習慣／研究／生活／加班，全部同一個形狀 |
| **拍子 `beat`** | 從完整的牌堆裡，依處境挑出一個問題與 2～4 個選項 |
| **修正器 `hook`** | 習慣、特質、流派、制度塌縮成同一個東西，核心只做 `fold()` |

### 年循環：五拍

```
1. 開年   收入入帳 → 今年的題材 → 翻訊號牌 → 擲行動點 →（心態過高會先替你花掉一點）
2. 事件   抽牌（尚未實作，見「遷移路徑」）
3. 出牌   一點行動點問一個問題（beat），2～4 個選項
4. 年中   崩盤／狂牛才有，一次免費行動
5. 結算   股利 → 價格走完全年 → 融資利息與斷頭 → 心態 → 生活 → 習慣進退
```

---

## 各系統的設計理由

### 標的：13 檔，各自有價格序列

5 檔核心（債券 ETF、高股息 ETF、市值型 ETF、權值龍頭、冷門小型股）+ 8 檔題材股。

```
價格報酬 = beta × 大盤 + alpha − 殖利率
```

殖利率從價格裡扣掉、股利另外入現金——**除息當天股價直接扣掉股利，帳面一毛沒賺**。
所以總報酬乾淨（`beta × 大盤 + alpha`），但帳面價格看得到除息的坑。

題材股每年有一檔是「今年的」（alpha +18），而**去年那檔會泡沫破掉**（alpha −18）。
這是新骨架最主要的張力來源：你手上很可能還抱著去年的航運。

### 訊號牌：資訊是可解讀的物件，不是一句「偏多」

15 張牌，每張對五種體制有不同權重。年初依**當年真實體制**加權抽 2 張
（有「每季讀財報」的習慣多翻一張），牌池重疊所以有雜訊。

玩家跨局學得會這張表——**這是這版唯一的長期成長，而且不需要解鎖系統**。

> **踩過的坑**：權重表原本有很多 0。一張在崩盤年權重為 0 的牌一旦出現，
> 就等於直接排除崩盤——貝氏更新吃到 0 是決定性的。
> 實測會讀表的玩家勝率 98%、贏對照組 120.7%，那就是支配策略。
> 現在抽牌時給每個體制一個地板 `SIG_FLOOR=1.6`，勝率降到 71%。

題材訊號另外給，準確率刻意只有 55%——追題材不該是穩賺的。

### 心態：不給數值懲罰，而是動選項

```
tilt ≥ 80  手癢    年初自動花掉一點行動點去追熱門題材（「這一點不是你決定的」）
tilt ≤ 20  不敢    買入牌整年消失
tilt < 35 或 > 85  行動點 −1
```

「只在收盤後下單」這個習慣的效果，就是**免疫手癢**。

> **踩過的坑**：心態原本吃絕對報酬，變成一個只升不降的棘輪——
> 多頭年年 +20%，很快就頂在 80，實測 **51.7% 的年份都在手癢**。
> 改成吃「相對大盤」的表現，再加每年往 55 回歸。
> 心態的來源本來就是比較：大盤漲 35% 你漲 30%，那不是得意，那是焦慮。

改完之後，心態的擺盪幅度隨集中度放大：

| 持股 | 手癢 | 不敢 | tilt p10/p50/p90 |
|---|---|---|---|
| 市值型 ETF | 0.1% | 0.0% | 50 / 56 / 64 |
| 權值龍頭 | 11.4% | 0.0% | 47 / 62 / 81 |
| 追題材 | 26.1% | 10.1% | 20 / 62 / 92 |
| 冷門小型股 | 9.5% | 2.6% | 32 / 55 / 80 |

買 ETF 抱著的人情緒是平的——**這正是它最大的優點**，而且是機制自己長出來的，不是寫死的。

### 拍子：一次一個問題

`moves()` 是完整的合法空間，模擬器要靠它跑遍所有路徑。但直接攤在畫面上，
實測**平均一次 42 個選項、最多 58 個**——那是報表，不是人生模擬器。

所以加一層 `beat()`：依當下處境挑出**一個問題、2～4 個選項**。

```
航運概念股已經幫你賺了 62%。你每天打開軟體第一個看的就是它。
  ▸ 賣一半，先把本金抽回來      剩下的讓它跑
  ▸ 全部落袋                    證交稅 0.3%，但你不會再看它了
  ▸ 一張都不賣                  會賺的部位不該賣——你是這樣告訴自己的
```

規矩跟事件卡同一條（`index.html:723`）：**選項必須是那個處境裡你真的能做的事，
而且各有各的後果**。沒有「保守／照常／全力」這種難度旋鈕。

`BEATS` 的順序就是優先序——帳上那筆賺了六成的部位，比「今年要不要開始運動」
更該先問你。目前七種處境：停利、停損、生活撐不住、錢放哪裡、習慣、研究、加班。
加一種＝往 `BEATS` 加一列。

實測每題平均 **3.2 個選項**（最多 5、最少 2），每一點行動點都有東西可問。
處境分布：錢放哪裡 39%、習慣 24%、停利 22%、停損 11%、研究 3%、生活 1%。

兩個刻意的設計：

- **不攤十三檔標的**。「錢放哪裡」只給三個：今年市場在講的那檔、一檔穩的、
  你已經有的那檔加碼。
- **融資不是常駐按鈕**。只有 `tilt >= 70`（你正得意）的時候它才會自己冒出來——
  那才是它真正出現在人生裡的樣子。

> **踩過的坑**：`beat()` 原本只拿 `moves()` 驗選項合不合法，但選項用的是六成、一半。
> 可投入 0.8 萬時六成只有 0.48 萬，低於最小成交金額——那顆按鈕在畫面上就是
> 按了沒反應。現在連金額一起驗。

### 年中窗口：貪婪與恐懼各測一次

崩盤年給「全部砍掉／抱著不看／加碼攤平」，狂牛年給「獲利了結一半／續抱／再加碼」。
一次免費行動。

年初就把整年報酬抽好，所以半路價格 `p × (1 + r × 0.62)` 是**真實路徑的一半**，
不是另外編出來的數字。跌停鎖死走 `escape` hook：砍不砍得掉真的要擲，
基礎機率由標的流動性決定，「停損寫在紙上」+20。

### 交易成本：從查表改成實扣

賣出 0.3% 證交稅 + 0.0855% 手續費，在 `sell()` 當場扣。
舊版的 `TURNOVER` 查表（存股 0.15、波段 2.6、當沖 9.5）整個刪掉——
**換手率現在是玩家行為的結果，不是流派的屬性**。

### 融資：真的算維持率

`debt`、7.5% 年息、維持率跌破 130% 強制平倉。沒有 `gross <= -60` 這種魔術數字。
三十三歲還在散戶這一層就永久關閉——不是規則禁止你，是你輸不起了。

### 習慣：三階，槽位仍然有限

嘗試（1 年）→ 習慣（3 年）→ 固化（6 年，不再需要餵，也不再衰退）。
槽位 2 → 3 → 4，固化也佔位子。沒餵就退，退到 0 斷掉，斷掉要從頭來。

> **踩過的坑**：習慣固化之後行動點沒事可幹，策略只好狂加班——
> 實測 18.3% 的人因此垮掉，而且現金在帳上躺一整年。
> 那正是舊版「build 蓋完就沒事做」的老毛病換個位置重演。
> 加了 `scout`（1 點行動，換一個「這檔今年會不會贏大盤」的判斷，
> 準確率 62%＋讀財報加成）。它永遠有用，垮掉率歸零。

---

## 怎麼擴充

這是換骨架的主要理由。三種內容各只要**一筆資料**，核心一行都不用改。

**加一檔標的** —— 往 `CORE` 加一列：

```js
{id:'reit', n:'REITs', beta:0.55, aMu:0.8, aSd:9, div:4.2, liq:0.8}
```

**加一張訊號牌** —— 往 `SIGNALS` 加一列：

```js
{id:'ipo_rush', t:'今年 IPO 排隊排到明年', w:{boom:5,bull:3,chop:1,bear:0,crash:1}}
```

**加一個習慣** —— 一筆資料 + 一行 `on()`：

```js
tea: {n:'跟營業員喝咖啡', fx:'風向判斷準確率 +18%'}
on('signals','tea', (n,c)=> n + (hb(c.g,'tea')>=0.75?1:0));
```

特質、流派、制度全部是同一件事——`on(hook, id, fn)`。
現有的 hook：`signals` `tiltdelta` `escape` `itch` `life` `investable`。

---

## 遷移路徑

### 可以機械式轉過來的

**33 張事件卡**。卡片格式（`n` / `d` / `o[]` / `need` / `block` / `p` / `g` / `b`）完全保留，
只有效果欄位要改對應：

| 舊 | 新 |
|---|---|
| `nav: +7`（淨值百分比） | `cash: +7` 或對指定標的加減碼 |
| `mood` | `tilt` |
| `life` | `life`（不變） |
| `hab: {log:2}` | `hab: {log:2}`（不變） |
| `trust` | 受託系統尚未移植，暫時丟棄 |

**流派、特質、資本階級、受託關係**：全部改寫成 `on()` 註冊。
這是機械工作，但每一條都要重新校準——舊版的數值是為 `settle()` 那條管線調的。

### 會消失的東西

- `POSN` 的六段風險滑桿 → 由 13 檔標的取代
- `TURNOVER` 換手率查表 → 由實際交易取代
- `hintAcc()` 的一句「偏多／偏空」 → 由訊號牌取代
- `settle()` 的 15 個特例分支 → 由 hook 取代
- 骰子分配到習慣槽位的 UI → 由拍子取代

### 還沒做的

- **事件卡尚未實作在內核裡**（第 2 拍是空的）
- 結局只有 10 種佔位，舊版 24 種還沒移
- 受託關係、資本階級、同儕、鬼影、戰帖碼、分享圖都還沒移
- `play.html` 是骨架試玩頁，不是完成品：沒有主題切換、沒有時間軸、沒有圖鑑
- `kernel-sim.js` 的策略走 `moves()` 而不是 `beat()`——量的是完整空間的平衡，
  不是玩家實際被問到那 3.2 個選項時的平衡。這兩者可能不一樣。

---

## 實測

`node kernel-sim.js 1000`，1000 顆種子 × 25 年 × 7 種策略。
策略一律不呼叫內核的亂數，只讀狀態——否則會偷走種子流，結果不可比。

**大盤校準**：算術年均 11.07%、幾何年化 7.52%、每局崩盤 2.23 次。
幾何遠低於算術是刻意的——波動本身就在吃掉報酬。

> 舊版的體制區間（崩盤到 −58%）實測只有幾何 5.34%，崩盤尾巴太長把複利吃掉了。
> 五個區間都調過之後才回到 7.5 附近。

**本金**：二十五年存下來約 496 萬。

> **踩過的坑**：收入原本用「薪水」而不是「一年存得下來的錢」，
> 結果是**「空手」不投資也能走到 3082 萬、CAGR 22.3%，投資只貢獻 7%**。
> 存錢本身贏過投資，整款遊戲的核心決策就沒有意義了。

**對照組**＝一模一樣的每一筆錢、同一時點全部買市值型 ETF 並把股利再投入。
所以差距完全來自選擇，不是本金。

| 策略 | p10 | 中位數 | p90 | 贏對照組 | 勝率 | 歸零 | 斷頭 |
|---|---|---|---|---|---|---|---|
| 空手 | 543萬 | 577萬 | 613萬 | −51.4% | 14% | 0% | 0% |
| 市值型 ETF 抱到底 | 509萬 | 1199萬 | 2975萬 | **−0.3%** | 0% | 0% | 0% |
| 高股息存股 | 537萬 | 1087萬 | 2410萬 | −7.8% | 25% | 0% | 0% |
| 每年追當紅題材 | 144萬 | 925萬 | **5180萬** | −17.6% | 44% | 5.4% | 0% |
| 融資追題材 | 0萬 | 679萬 | 4856萬 | −36.7% | 38% | **27.7%** | 28.8% |
| 讀訊號配置 | 728萬 | 1497萬 | 3136萬 | **+28.5%** | 71% | 0% | 0% |
| 讀訊號＋看對才追題材 | 664萬 | 1514萬 | 3570萬 | +28.5% | 70% | 0.5% | 0% |

**「市值型 ETF 抱到底」對上自己的對照組是 −0.3%，剛好等於手續費。**
這個數字是整份模型的體檢：只要哪裡偷偷漏錢或偷偷生錢，它就不會落在 0 附近。

> **踩過的坑**：對照組原本當年就把股利買回去，玩家的要等隔年。
> 光是這個時間差，就讓買市值型抱到底的人對上自己的對照組輸 5.4%。

其他讀法：

- **技術有價，但不保證**：會讀訊號 +28.5%、勝率 71%。不是穩贏。
- **追題材是變異數選擇，不是期望值選擇**：中位數比 ETF 低（925 萬 vs 1199 萬），
  但 p90 最高（5180 萬）。誘惑是真的，期望值是負的。
- **融資會殺人**：27.7% 歸零、28.8% 斷過頭，但 p90 還有 4856 萬。
- **最強與第二名只差 1.01 倍**，沒有支配策略。最強策略的 p90/p10 ＝ 5.4 倍，
  同一種打法在不同種子下差 5 倍——種子有影響，但選擇沒有被雜訊淹沒。

---

## 已知缺陷

- `kernel-sim.js` 裡的「讀訊號」策略用**完整貝氏後驗**，等於知道整張權重表。
  那是技術上限的對照組，不是一般玩家做得到的事。真人拿到的優勢會低於 +28.5%。
- 結局判定只有 10 條佔位規則。
- `play-smoke.js` 只驗載入期與 API 面（用假 DOM），沒有真的驗版面與互動——
  那還是得自己開瀏覽器點一遍。
